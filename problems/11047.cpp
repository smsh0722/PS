#include <iostream>
using namespace std;

/* ci: coin idx
 * sum: current sum
 * */
int main( void )
{
    int N, K; // N: # of kinds, K: goal #
    cin >> N >> K;
    
    int arr[N];
    for ( int i = 0; i < N; i++ )
        cin >> arr[i];
    
    
}
